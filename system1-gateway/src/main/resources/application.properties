# Server Configuration
server.port=${PORT:8081}

# System 2 URL (Production URL hardcoded for Render deployment)
system2.url=https://system2-corebank-87wm.onrender.com

# Application Name
spring.application.name=system1-gateway

# CORS Configuration (for frontend - Production URL hardcoded)
spring.web.cors.allowed-origins=https://banking-ui-eryw.onrender.com
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

